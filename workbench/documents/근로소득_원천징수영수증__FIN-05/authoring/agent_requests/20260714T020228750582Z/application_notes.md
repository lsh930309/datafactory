# FIN-05 검증 복구 메모

- 기존 188개 schema binding에 렌더러 지원 generator를 각각 하나씩 추가했습니다.
- `det_000023`, `det_000024`는 같은 선택 항목의 기존 `use/value_region` 근거를 유지하면서 anchor 분류만 `field_value`로 정상화했습니다.
- schema, stylesheet, research report, uncertainty report, semantic path, field ID, anchor ID, style, 연구 근거와 관계 제약은 새로 만들거나 삭제하지 않았습니다.
- 이번 복구에서는 새 value pool이나 record 관계를 추가하지 않았습니다.

## 승인·적용

UI에서 8개 draft 산출물을 함께 검토한 뒤 승인하십시오. 승인 전에는 final authoring 파일에 복사하거나 덮어쓰지 마십시오.
